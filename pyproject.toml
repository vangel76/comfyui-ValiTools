[project]
name = "valitools"
description = "ValiTools - a collection of ComfyUI nodes. Currently: VSmartPrompt, a Dynamic Prompts node with a rich-text editor, reusable variables and post-run selection highlighting."
version = "1.7.0"
license = { file = "LICENSE" }

[tool.comfy]
DisplayName = "ValiTools"
Icon = ""
